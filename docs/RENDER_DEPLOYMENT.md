# 🎯 Render 部署指南（最简单的方式）

## 为什么选择 Render？

- ✅ **完全免费** - 免费套餐足够使用
- ✅ **零配置** - 从 GitHub 自动部署
- ✅ **自动 HTTPS** - 免费 SSL 证书
- ✅ **内置数据库** - 免费 PostgreSQL
- ✅ **自动重启** - 崩溃自动恢复
- ✅ **无需 CLI** - 网页操作，简单直观

---

## 📋 部署前检查

- ✅ 代码已推送到 GitHub
- ✅ 已准备好 DeepSeek API Key
- ✅ 已准备好 LangSmith API Key

您的 GitHub 仓库：
```
https://github.com/liwz0319-star/E_Business
```

---

## 🚀 部署步骤（5 分钟完成）

### 步骤 1: 注册/登录 Render

1. 访问：https://render.com
2. 点击 "Sign Up" 或 "Log In"
3. 使用 GitHub 账号登录（推荐）

### 步骤 2: 创建新的 Web Service

1. 登录后，点击 **"New +"** 按钮
2. 选择 **"Web Service"**

### 步骤 3: 连接 GitHub 仓库

1. 如果是第一次使用，点击 **"Connect GitHub"**
2. 授权 Render 访问您的 GitHub
3. 在仓库列表中找到并选择 **"E_Business"**
4. 点击 **"Connect"**

### 步骤 4: 配置部署设置

在配置页面填写以下信息：

#### **基本信息**

| 字段 | 值 |
|------|-----|
| **Name** | `e-business-api`（或任意名称）|
| **Region** | 选择离您最近的区域（如 Singapore）|
| **Branch** | `main` |

#### **构建和运行**

| 字段 | 值 |
|------|-----|
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `uvicorn app.main:app --host 0.0.0.0 --port $PORT` |

#### **工作目录**

**重要**: 设置 **Root Directory** 为 `backend`

因为应用代码在 `backend/` 目录下。

### 步骤 5: 添加环境变量

在 **"Advanced"** → **"Environment Variables"** 中添加：

```bash
# 必需的 API Keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here
LANGCHAIN_API_KEY=lsv2_pt_your_langsmith_key_here

# LangSmith 配置
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=e-business

# 应用配置
APP_ENV=production
DEBUG=false
```

### 步骤 6: 选择实例类型

- **Free** - 免费套餐（推荐开始使用）
  - 512 MB RAM
  - 0.1 CPU
  - 每月 750 小时

### 步骤 7: 部署

1. 点击 **"Create Web Service"**
2. 等待 2-3 分钟构建和部署
3. 实时日志会显示在页面下方

### 步骤 8: 获取应用 URL

部署成功后，Render 会给您一个 URL：
```
https://e-business-api.onrender.com
```

或者您自定义的名称。

---

## 🗄️ 添加 PostgreSQL 数据库（可选）

如果需要在 Render 上托管数据库：

### 步骤 1: 创建数据库

1. 在 Dashboard 中点击 **"New +"**
2. 选择 **"PostgreSQL"**
3. 配置：
   - **Database Name**: `e_business`
   - **User**: `e_business_user`
   - **Region**: 与应用相同
4. 选择 **Free** 套餐
5. 点击 **"Create Database"**

### 步骤 2: 连接数据库

1. 创建后，Render 会显示 **"Internal Connections"**
2. 复制 **"Database URL"**
3. 在 Web Service 的环境变量中添加：
   ```bash
   DATABASE_URL=复制这里的数据库URL
   ```

### 步骤 3: 运行数据库迁移

部署完成后，在 Render Shell 中运行：

```bash
# 在 Render Dashboard → Web Service → Shell
cd backend
python -m alembic upgrade head
```

---

## ✅ 验证部署

部署完成后，验证以下端点：

### 1. 健康检查

```bash
curl https://your-app.onrender.com/health
```

应该返回：
```json
{"status": "ok"}
```

### 2. 检查 LangSmith 配置

```bash
curl https://your-app.onrender.com/api/v1/debug/langsmith
```

应该返回：
```json
{
  "enabled": true,
  "project": "e-business",
  "api_key_configured": true
}
```

### 3. 测试文案生成 API

```bash
curl -X POST https://your-app.onrender.com/api/v1/copywriting/generate \
  -H "Content-Type: application/json" \
  -d '{
    "product_name": "智能手表",
    "features": ["GPS定位", "心率监测", "防水"]
  }'
```

---

## 📊 监控和日志

### 查看实时日志

1. 进入您的 Web Service
2. 点击 **"Logs"** 标签
3. 实时查看应用日志

### 查看部署历史

1. 在 Web Service 页面
2. 点击 **"Events"** 标签
3. 查看所有部署记录

### 监控指标

1. 点击 **"Metrics"** 标签
2. 查看 CPU、内存使用情况

---

## 🔄 自动部署

Render 会自动检测 GitHub 的推送并重新部署：

```bash
# 本地修改代码
git add .
git commit -m "Update feature"
git push origin main
```

Render 会自动：
1. 检测到新提交
2. 拉取最新代码
3. 构建和部署
4. 发送邮件通知

---

## 🌐 自定义域名（可选）

### 步骤 1: 准备域名

1. 购买域名（或使用已有域名）
2. 在 DNS 提供商处添加 CNAME 记录

### 步骤 2: 在 Render 添加域名

1. Web Service → **"Settings"**
2. **"Custom Domain"**
3. 输入域名（如 `api.yourdomain.com`）
4. Render 会自动配置 SSL 证书

---

## 🎯 LangSmith 监控配置

部署后，LangSmith 会自动开始追踪：

### 访问 LangSmith

1. 访问：https://smith.langchain.com/projects
2. 找到 **"e-business"** 项目
3. 点击查看所有追踪记录

### 可追踪的工作流

- ✅ **Copywriting Agent**: Plan → Draft → Critique → Finalize
- ✅ **Image Agent**: Optimize Prompt → Generate Image → Persist

### 查看实时追踪

1. 触发一个 API 请求
2. 在 LangSmith 页面刷新
3. 新的追踪记录会立即出现

---

## 💰 免费套餐限制

Render 免费套餐包括：

| 资源 | 限制 |
|------|------|
| **RAM** | 512 MB |
| **CPU** | 0.1 CPU |
| **时间** | 750 小时/月 |
| **数据库** | 1GB PostgreSQL |
| **带宽** | 100GB/月 |

### 休眠机制

免费套餐有以下限制：

- ⚠️ **15 分钟无请求自动休眠**
- ⚠️ **休眠后首次请求需要 ~30 秒启动**
- ✅ **付费套餐可保持常驻**

### 解决休眠问题

如果需要 24/7 运行：

1. 升级到 **Starter** 套餐（$7/月）
2. 或使用 **Ping 服务** 保持唤醒：
   ```bash
   # 每 10 分钟 ping 一次
   */10 * * * * curl https://your-app.onrender.com/health
   ```

---

## 🔧 故障排除

### 问题 1: 部署失败

**检查**:
- 查看部署日志中的错误信息
- 确认 `backend/requirements.txt` 存在
- 确认 `backend/app/main.py` 存在

**解决**:
```bash
# 本地测试启动命令
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 问题 2: 环境变量未生效

**检查**:
- 在 Render Dashboard 中确认环境变量已添加
- 确认变量名拼写正确（区分大小写）

**解决**:
- 删除并重新添加环境变量
- 触发新的部署

### 问题 3: 数据库连接失败

**检查**:
- 确认数据库已创建
- 确认 `DATABASE_URL` 正确

**解决**:
- 使用 Render 的 **Internal Connection** 字符串
- 确认数据库和应用在同一区域

### 问题 4: 应用休眠

**现象**: 首次访问很慢（~30秒）

**解决**:
- 这是免费套餐的正常行为
- 升级到付费套餐可解决
- 或使用外部 ping 服务保持唤醒

---

## 📚 相关资源

- [Render 官方文档](https://render.com/docs)
- [Render 定价](https://render.com/pricing)
- [环境变量指南](https://render.com/docs/env-vars)
- [部署指南](https://render.com/docs/deploy-node-express-app)

---

## 🎉 完成！

部署成功后：

1. ✅ 您的应用运行在 Render 上
2. ✅ 访问应用 URL 开始使用
3. ✅ LangSmith 自动监控所有 AI 调用
4. ✅ GitHub 推送自动触发部署

**URL 获取**: 在 Render Dashboard 中查看您的应用 URL

**开始使用吧！** 🚀
